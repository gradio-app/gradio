---
"gradio": minor
---

feat: Add `gr.Workflow` and `gr.WorkflowCanvas` for building AI pipelines visually. `gr.Workflow` accepts `bind` and `edges` parameters to wire Python functions into a canvas, with automatic persistence to `workflow.json`.
