---
"@gradio/workflowcanvas": minor
"gradio": minor
---

feat:Run `gr.Workflow` subgraphs via the Gradio API — each output (subject) is exposed as a named endpoint reusing `/info`, `/call`, and `/api`, with a "View API" panel in the canvas
