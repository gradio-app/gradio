---
"@gradio/core": minor
"@gradio/workflowcanvas": minor
"gradio": minor
---

feat: Add `gr.Workflow` and `gr.WorkflowCanvas` for building AI pipelines visually. `gr.Workflow` accepts `graph`, `bind`, and `edges` parameters to wire Python functions into a canvas, with automatic persistence to the graph JSON file.
