---
"gradio": minor
---

feat:Add display controls for examples. Note: `gr.Interface` now hides the `additional_inputs` columns in its examples table by default; their values are still loaded when an example is selected.
