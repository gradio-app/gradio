---
"gradio": minor
"gradio_client": minor
---

feat:oauth: let API callers supply a token for endpoints that take a `gr.OAuthToken`, via `Client(..., oauth_token=...)` — sent only to endpoints that declare they need one
