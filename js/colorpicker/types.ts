export interface ColorPickerProps {
	value: string;
	info: string;
}

export interface ColorPickerEvents {
	change: never;
	input: never;
	submit: never;
	focus: never;
	blur: never;
	clear_status: any;
}
