import { SvelteComponentDev, init, safe_not_equal, dispatch_dev, validate_slots, element, src_url_equal, attr_dev, toggle_class, add_location, insert_dev, noop, detach_dev } from './svelte/svelte-internal.js';

const Image_svelte_svelte_type_style_lang = '';

/* Users/peterallen/Projects/gradio/js/image/example/Image.svelte generated by Svelte v4.0.0 */

const file = "Users/peterallen/Projects/gradio/js/image/example/Image.svelte";

function create_fragment(ctx) {
	let img;
	let img_src_value;

	const block = {
		c: function create() {
			img = element("img");
			if (!src_url_equal(img.src, img_src_value = /*samples_dir*/ ctx[1] + /*value*/ ctx[0])) attr_dev(img, "src", img_src_value);
			attr_dev(img, "class", "s-CkcJeeZMATzL");
			toggle_class(img, "table", /*type*/ ctx[2] === "table");
			toggle_class(img, "gallery", /*type*/ ctx[2] === "gallery");
			toggle_class(img, "selected", /*selected*/ ctx[3]);
			add_location(img, file, 9, 0, 182);
		},
		l: function claim(nodes) {
			throw new Error("options.hydrate only works if the component was compiled with the `hydratable: true` option");
		},
		m: function mount(target, anchor) {
			insert_dev(target, img, anchor);
		},
		p: function update(ctx, [dirty]) {
			if (dirty & /*samples_dir, value*/ 3 && !src_url_equal(img.src, img_src_value = /*samples_dir*/ ctx[1] + /*value*/ ctx[0])) {
				attr_dev(img, "src", img_src_value);
			}

			if (dirty & /*type*/ 4) {
				toggle_class(img, "table", /*type*/ ctx[2] === "table");
			}

			if (dirty & /*type*/ 4) {
				toggle_class(img, "gallery", /*type*/ ctx[2] === "gallery");
			}

			if (dirty & /*selected*/ 8) {
				toggle_class(img, "selected", /*selected*/ ctx[3]);
			}
		},
		i: noop,
		o: noop,
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(img);
			}
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_fragment.name,
		type: "component",
		source: "",
		ctx
	});

	return block;
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	validate_slots('Image', slots, []);
	let { value } = $$props;
	let { samples_dir } = $$props;
	let { type } = $$props;
	let { selected = false } = $$props;

	$$self.$$.on_mount.push(function () {
		if (value === undefined && !('value' in $$props || $$self.$$.bound[$$self.$$.props['value']])) {
			console.warn("<Image> was created without expected prop 'value'");
		}

		if (samples_dir === undefined && !('samples_dir' in $$props || $$self.$$.bound[$$self.$$.props['samples_dir']])) {
			console.warn("<Image> was created without expected prop 'samples_dir'");
		}

		if (type === undefined && !('type' in $$props || $$self.$$.bound[$$self.$$.props['type']])) {
			console.warn("<Image> was created without expected prop 'type'");
		}
	});

	const writable_props = ['value', 'samples_dir', 'type', 'selected'];

	Object.keys($$props).forEach(key => {
		if (!~writable_props.indexOf(key) && key.slice(0, 2) !== '$$' && key !== 'slot') console.warn(`<Image> was created with unknown prop '${key}'`);
	});

	$$self.$$set = $$props => {
		if ('value' in $$props) $$invalidate(0, value = $$props.value);
		if ('samples_dir' in $$props) $$invalidate(1, samples_dir = $$props.samples_dir);
		if ('type' in $$props) $$invalidate(2, type = $$props.type);
		if ('selected' in $$props) $$invalidate(3, selected = $$props.selected);
	};

	$$self.$capture_state = () => ({ value, samples_dir, type, selected });

	$$self.$inject_state = $$props => {
		if ('value' in $$props) $$invalidate(0, value = $$props.value);
		if ('samples_dir' in $$props) $$invalidate(1, samples_dir = $$props.samples_dir);
		if ('type' in $$props) $$invalidate(2, type = $$props.type);
		if ('selected' in $$props) $$invalidate(3, selected = $$props.selected);
	};

	if ($$props && "$$inject" in $$props) {
		$$self.$inject_state($$props.$$inject);
	}

	return [value, samples_dir, type, selected];
}

class Image extends SvelteComponentDev {
	constructor(options) {
		super(options);

		init(this, options, instance, create_fragment, safe_not_equal, {
			value: 0,
			samples_dir: 1,
			type: 2,
			selected: 3
		});

		dispatch_dev("SvelteRegisterComponent", {
			component: this,
			tagName: "Image",
			options,
			id: create_fragment.name
		});
	}

	get value() {
		throw new Error("<Image>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set value(value) {
		throw new Error("<Image>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	get samples_dir() {
		throw new Error("<Image>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set samples_dir(value) {
		throw new Error("<Image>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	get type() {
		throw new Error("<Image>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set type(value) {
		throw new Error("<Image>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	get selected() {
		throw new Error("<Image>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set selected(value) {
		throw new Error("<Image>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}
}

const Image$1 = Image;

export { Image$1 as default };
//# sourceMappingURL=index-04a3bb38.js.map
