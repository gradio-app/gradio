import { SvelteComponentDev, init, safe_not_equal, dispatch_dev, globals, validate_slots, empty, insert_dev, noop, detach_dev, element, attr_dev, toggle_class, add_location, listen_dev, run_all, ensure_array_like_dev, space, append_dev, destroy_each, text, set_data_dev } from './svelte/svelte-internal.js';
import { d as dsvFormat } from './dsv-a37bb3db.js';

var csv = dsvFormat(",");

csv.parse;
var csvParseRows = csv.parseRows;
csv.format;
csv.formatBody;
csv.formatRows;
csv.formatRow;
csv.formatValue;

var tsv = dsvFormat("\t");

tsv.parse;
var tsvParseRows = tsv.parseRows;
tsv.format;
tsv.formatBody;
tsv.formatRows;
tsv.formatRow;
tsv.formatValue;

const Dataframe_svelte_svelte_type_style_lang = '';

/* Users/peterallen/Projects/gradio/js/dataframe/example/Dataframe.svelte generated by Svelte v4.0.0 */

const { Error: Error_1, console: console_1 } = globals;
const file = "Users/peterallen/Projects/gradio/js/dataframe/example/Dataframe.svelte";

function get_each_context(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[11] = list[i];
	child_ctx[13] = i;
	return child_ctx;
}

function get_each_context_1(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[14] = list[i];
	child_ctx[16] = i;
	return child_ctx;
}

// (48:0) {#if loaded}
function create_if_block(ctx) {
	let div;
	let mounted;
	let dispose;

	function select_block_type(ctx, dirty) {
		if (typeof /*loaded_value*/ ctx[6] === "string") return create_if_block_1;
		return create_else_block;
	}

	let current_block_type = select_block_type(ctx);
	let if_block = current_block_type(ctx);

	const block = {
		c: function create() {
			div = element("div");
			if_block.c();
			attr_dev(div, "class", "s-UGKq17R8LHdi");
			toggle_class(div, "table", /*type*/ ctx[1] === "table");
			toggle_class(div, "gallery", /*type*/ ctx[1] === "gallery");
			toggle_class(div, "selected", /*selected*/ ctx[2]);
			add_location(div, file, 55, 1, 1495);
		},
		m: function mount(target, anchor) {
			insert_dev(target, div, anchor);
			if_block.m(div, null);

			if (!mounted) {
				dispose = [
					listen_dev(div, "mouseenter", /*mouseenter_handler*/ ctx[9], false, false, false, false),
					listen_dev(div, "mouseleave", /*mouseleave_handler*/ ctx[10], false, false, false, false)
				];

				mounted = true;
			}
		},
		p: function update(ctx, dirty) {
			if (current_block_type === (current_block_type = select_block_type(ctx)) && if_block) {
				if_block.p(ctx, dirty);
			} else {
				if_block.d(1);
				if_block = current_block_type(ctx);

				if (if_block) {
					if_block.c();
					if_block.m(div, null);
				}
			}

			if (dirty & /*type*/ 2) {
				toggle_class(div, "table", /*type*/ ctx[1] === "table");
			}

			if (dirty & /*type*/ 2) {
				toggle_class(div, "gallery", /*type*/ ctx[1] === "gallery");
			}

			if (dirty & /*selected*/ 4) {
				toggle_class(div, "selected", /*selected*/ ctx[2]);
			}
		},
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(div);
			}

			if_block.d();
			mounted = false;
			run_all(dispose);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block.name,
		type: "if",
		source: "(48:0) {#if loaded}",
		ctx
	});

	return block;
}

// (60:2) {:else}
function create_else_block(ctx) {
	let table;
	let t;
	let each_value = ensure_array_like_dev(/*loaded_value*/ ctx[6].slice(0, 3));
	let each_blocks = [];

	for (let i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
	}

	let if_block = /*value*/ ctx[0].length > 3 && create_if_block_2(ctx);

	const block = {
		c: function create() {
			table = element("table");

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			t = space();
			if (if_block) if_block.c();
			attr_dev(table, "class", " s-UGKq17R8LHdi");
			add_location(table, file, 65, 3, 1745);
		},
		m: function mount(target, anchor) {
			insert_dev(target, table, anchor);

			for (let i = 0; i < each_blocks.length; i += 1) {
				if (each_blocks[i]) {
					each_blocks[i].m(table, null);
				}
			}

			append_dev(table, t);
			if (if_block) if_block.m(table, null);
		},
		p: function update(ctx, dirty) {
			if (dirty & /*loaded_value*/ 64) {
				each_value = ensure_array_like_dev(/*loaded_value*/ ctx[6].slice(0, 3));
				let i;

				for (i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
					} else {
						each_blocks[i] = create_each_block(child_ctx);
						each_blocks[i].c();
						each_blocks[i].m(table, t);
					}
				}

				for (; i < each_blocks.length; i += 1) {
					each_blocks[i].d(1);
				}

				each_blocks.length = each_value.length;
			}

			if (/*value*/ ctx[0].length > 3) {
				if (if_block) {
					if_block.p(ctx, dirty);
				} else {
					if_block = create_if_block_2(ctx);
					if_block.c();
					if_block.m(table, null);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}
		},
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(table);
			}

			destroy_each(each_blocks, detaching);
			if (if_block) if_block.d();
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_else_block.name,
		type: "else",
		source: "(60:2) {:else}",
		ctx
	});

	return block;
}

// (58:2) {#if typeof loaded_value === "string"}
function create_if_block_1(ctx) {
	let t;

	const block = {
		c: function create() {
			t = text(/*loaded_value*/ ctx[6]);
		},
		m: function mount(target, anchor) {
			insert_dev(target, t, anchor);
		},
		p: function update(ctx, dirty) {
			if (dirty & /*loaded_value*/ 64) set_data_dev(t, /*loaded_value*/ ctx[6]);
		},
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(t);
			}
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block_1.name,
		type: "if",
		source: "(58:2) {#if typeof loaded_value === \\\"string\\\"}",
		ctx
	});

	return block;
}

// (64:6) {#each row.slice(0, 3) as cell, j}
function create_each_block_1(ctx) {
	let td;
	let t_value = /*cell*/ ctx[14] + "";
	let t;

	const block = {
		c: function create() {
			td = element("td");
			t = text(t_value);
			attr_dev(td, "class", "s-UGKq17R8LHdi");
			add_location(td, file, 69, 7, 1867);
		},
		m: function mount(target, anchor) {
			insert_dev(target, td, anchor);
			append_dev(td, t);
		},
		p: function update(ctx, dirty) {
			if (dirty & /*loaded_value*/ 64 && t_value !== (t_value = /*cell*/ ctx[14] + "")) set_data_dev(t, t_value);
		},
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(td);
			}
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_each_block_1.name,
		type: "each",
		source: "(64:6) {#each row.slice(0, 3) as cell, j}",
		ctx
	});

	return block;
}

// (67:6) {#if row.length > 3}
function create_if_block_3(ctx) {
	let td;

	const block = {
		c: function create() {
			td = element("td");
			td.textContent = "…";
			attr_dev(td, "class", "s-UGKq17R8LHdi");
			add_location(td, file, 72, 7, 1931);
		},
		m: function mount(target, anchor) {
			insert_dev(target, td, anchor);
		},
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(td);
			}
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block_3.name,
		type: "if",
		source: "(67:6) {#if row.length > 3}",
		ctx
	});

	return block;
}

// (62:4) {#each loaded_value.slice(0, 3) as row, i}
function create_each_block(ctx) {
	let tr;
	let t;
	let each_value_1 = ensure_array_like_dev(/*row*/ ctx[11].slice(0, 3));
	let each_blocks = [];

	for (let i = 0; i < each_value_1.length; i += 1) {
		each_blocks[i] = create_each_block_1(get_each_context_1(ctx, each_value_1, i));
	}

	let if_block = /*row*/ ctx[11].length > 3 && create_if_block_3(ctx);

	const block = {
		c: function create() {
			tr = element("tr");

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			t = space();
			if (if_block) if_block.c();
			add_location(tr, file, 67, 5, 1814);
		},
		m: function mount(target, anchor) {
			insert_dev(target, tr, anchor);

			for (let i = 0; i < each_blocks.length; i += 1) {
				if (each_blocks[i]) {
					each_blocks[i].m(tr, null);
				}
			}

			append_dev(tr, t);
			if (if_block) if_block.m(tr, null);
		},
		p: function update(ctx, dirty) {
			if (dirty & /*loaded_value*/ 64) {
				each_value_1 = ensure_array_like_dev(/*row*/ ctx[11].slice(0, 3));
				let i;

				for (i = 0; i < each_value_1.length; i += 1) {
					const child_ctx = get_each_context_1(ctx, each_value_1, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
					} else {
						each_blocks[i] = create_each_block_1(child_ctx);
						each_blocks[i].c();
						each_blocks[i].m(tr, t);
					}
				}

				for (; i < each_blocks.length; i += 1) {
					each_blocks[i].d(1);
				}

				each_blocks.length = each_value_1.length;
			}

			if (/*row*/ ctx[11].length > 3) {
				if (if_block) ; else {
					if_block = create_if_block_3(ctx);
					if_block.c();
					if_block.m(tr, null);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}
		},
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(tr);
			}

			destroy_each(each_blocks, detaching);
			if (if_block) if_block.d();
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_each_block.name,
		type: "each",
		source: "(62:4) {#each loaded_value.slice(0, 3) as row, i}",
		ctx
	});

	return block;
}

// (72:4) {#if value.length > 3}
function create_if_block_2(ctx) {
	let div;

	const block = {
		c: function create() {
			div = element("div");
			attr_dev(div, "class", "overlay s-UGKq17R8LHdi");
			toggle_class(div, "odd", /*index*/ ctx[3] % 2 != 0);
			toggle_class(div, "even", /*index*/ ctx[3] % 2 == 0);
			toggle_class(div, "button", /*type*/ ctx[1] === "gallery");
			add_location(div, file, 77, 5, 2009);
		},
		m: function mount(target, anchor) {
			insert_dev(target, div, anchor);
		},
		p: function update(ctx, dirty) {
			if (dirty & /*index*/ 8) {
				toggle_class(div, "odd", /*index*/ ctx[3] % 2 != 0);
			}

			if (dirty & /*index*/ 8) {
				toggle_class(div, "even", /*index*/ ctx[3] % 2 == 0);
			}

			if (dirty & /*type*/ 2) {
				toggle_class(div, "button", /*type*/ ctx[1] === "gallery");
			}
		},
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(div);
			}
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block_2.name,
		type: "if",
		source: "(72:4) {#if value.length > 3}",
		ctx
	});

	return block;
}

function create_fragment(ctx) {
	let if_block_anchor;
	let if_block = /*loaded*/ ctx[4] && create_if_block(ctx);

	const block = {
		c: function create() {
			if (if_block) if_block.c();
			if_block_anchor = empty();
		},
		l: function claim(nodes) {
			throw new Error_1("options.hydrate only works if the component was compiled with the `hydratable: true` option");
		},
		m: function mount(target, anchor) {
			if (if_block) if_block.m(target, anchor);
			insert_dev(target, if_block_anchor, anchor);
		},
		p: function update(ctx, [dirty]) {
			if (/*loaded*/ ctx[4]) {
				if (if_block) {
					if_block.p(ctx, dirty);
				} else {
					if_block = create_if_block(ctx);
					if_block.c();
					if_block.m(if_block_anchor.parentNode, if_block_anchor);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}
		},
		i: noop,
		o: noop,
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(if_block_anchor);
			}

			if (if_block) if_block.d(detaching);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_fragment.name,
		type: "component",
		source: "",
		ctx
	});

	return block;
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	validate_slots('Dataframe', slots, []);
	let { gradio } = $$props;
	let { value } = $$props;
	let { samples_dir } = $$props;
	let { type } = $$props;
	let { selected = false } = $$props;
	let { index } = $$props;
	let hovered = false;
	let loaded_value = value;
	let loaded = Array.isArray(loaded_value);

	$$self.$$.on_mount.push(function () {
		if (gradio === undefined && !('gradio' in $$props || $$self.$$.bound[$$self.$$.props['gradio']])) {
			console_1.warn("<Dataframe> was created without expected prop 'gradio'");
		}

		if (value === undefined && !('value' in $$props || $$self.$$.bound[$$self.$$.props['value']])) {
			console_1.warn("<Dataframe> was created without expected prop 'value'");
		}

		if (samples_dir === undefined && !('samples_dir' in $$props || $$self.$$.bound[$$self.$$.props['samples_dir']])) {
			console_1.warn("<Dataframe> was created without expected prop 'samples_dir'");
		}

		if (type === undefined && !('type' in $$props || $$self.$$.bound[$$self.$$.props['type']])) {
			console_1.warn("<Dataframe> was created without expected prop 'type'");
		}

		if (index === undefined && !('index' in $$props || $$self.$$.bound[$$self.$$.props['index']])) {
			console_1.warn("<Dataframe> was created without expected prop 'index'");
		}
	});

	const writable_props = ['gradio', 'value', 'samples_dir', 'type', 'selected', 'index'];

	Object.keys($$props).forEach(key => {
		if (!~writable_props.indexOf(key) && key.slice(0, 2) !== '$$' && key !== 'slot') console_1.warn(`<Dataframe> was created with unknown prop '${key}'`);
	});

	const mouseenter_handler = () => $$invalidate(5, hovered = true);
	const mouseleave_handler = () => $$invalidate(5, hovered = false);

	$$self.$$set = $$props => {
		if ('gradio' in $$props) $$invalidate(7, gradio = $$props.gradio);
		if ('value' in $$props) $$invalidate(0, value = $$props.value);
		if ('samples_dir' in $$props) $$invalidate(8, samples_dir = $$props.samples_dir);
		if ('type' in $$props) $$invalidate(1, type = $$props.type);
		if ('selected' in $$props) $$invalidate(2, selected = $$props.selected);
		if ('index' in $$props) $$invalidate(3, index = $$props.index);
	};

	$$self.$capture_state = () => ({
		csvParseRows,
		tsvParseRows,
		gradio,
		value,
		samples_dir,
		type,
		selected,
		index,
		hovered,
		loaded_value,
		loaded
	});

	$$self.$inject_state = $$props => {
		if ('gradio' in $$props) $$invalidate(7, gradio = $$props.gradio);
		if ('value' in $$props) $$invalidate(0, value = $$props.value);
		if ('samples_dir' in $$props) $$invalidate(8, samples_dir = $$props.samples_dir);
		if ('type' in $$props) $$invalidate(1, type = $$props.type);
		if ('selected' in $$props) $$invalidate(2, selected = $$props.selected);
		if ('index' in $$props) $$invalidate(3, index = $$props.index);
		if ('hovered' in $$props) $$invalidate(5, hovered = $$props.hovered);
		if ('loaded_value' in $$props) $$invalidate(6, loaded_value = $$props.loaded_value);
		if ('loaded' in $$props) $$invalidate(4, loaded = $$props.loaded);
	};

	if ($$props && "$$inject" in $$props) {
		$$self.$inject_state($$props.$$inject);
	}

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*loaded, value, samples_dir, gradio*/ 401) {
			if (!loaded && typeof value === "string" && (/\.[a-zA-Z]+$/).test(value)) {
				fetch(samples_dir + value).then(v => v.text()).then(v => {
					try {
						if (value.endsWith("csv")) {
							const small_df = v.split("\n").slice(0, 4).map(v => v.split(",").slice(0, 4).join(",")).join("\n");
							$$invalidate(6, loaded_value = csvParseRows(small_df));
						} else if (value.endsWith("tsv")) {
							const small_df = v.split("\n").slice(0, 4).map(v => v.split("\t").slice(0, 4).join("\t")).join("\n");
							$$invalidate(6, loaded_value = tsvParseRows(small_df));
						} else {
							throw new Error(gradio.i18n("dataframe.incorrect_format"));
						}

						$$invalidate(4, loaded = true);
					} catch(e) {
						console.error(e);
					}
				}).catch(e => {
					$$invalidate(6, loaded_value = value);
					$$invalidate(4, loaded = true);
				});
			}
		}
	};

	return [
		value,
		type,
		selected,
		index,
		loaded,
		hovered,
		loaded_value,
		gradio,
		samples_dir,
		mouseenter_handler,
		mouseleave_handler
	];
}

class Dataframe extends SvelteComponentDev {
	constructor(options) {
		super(options);

		init(this, options, instance, create_fragment, safe_not_equal, {
			gradio: 7,
			value: 0,
			samples_dir: 8,
			type: 1,
			selected: 2,
			index: 3
		});

		dispatch_dev("SvelteRegisterComponent", {
			component: this,
			tagName: "Dataframe",
			options,
			id: create_fragment.name
		});
	}

	get gradio() {
		throw new Error_1("<Dataframe>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set gradio(value) {
		throw new Error_1("<Dataframe>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	get value() {
		throw new Error_1("<Dataframe>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set value(value) {
		throw new Error_1("<Dataframe>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	get samples_dir() {
		throw new Error_1("<Dataframe>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set samples_dir(value) {
		throw new Error_1("<Dataframe>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	get type() {
		throw new Error_1("<Dataframe>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set type(value) {
		throw new Error_1("<Dataframe>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	get selected() {
		throw new Error_1("<Dataframe>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set selected(value) {
		throw new Error_1("<Dataframe>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	get index() {
		throw new Error_1("<Dataframe>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set index(value) {
		throw new Error_1("<Dataframe>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}
}

const Dataframe$1 = Dataframe;

export { Dataframe$1 as default };
//# sourceMappingURL=index-da78ccd1.js.map
